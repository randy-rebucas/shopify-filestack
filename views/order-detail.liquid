{% layout 'layout' %}
<section>
    <div class="container">
        <div class="row">
            <div class="col-md-7">
                <h2>{{page_title}}</h2>
                <form method="post" action="/orders">
                    <input type="hidden" name="orderId" value="{{data.order.id}}"/>
                    <legend>Contact Information</legend>
                        <div class="form-group">
                            <label for="contact_email">Email</label>
                            <input type="email" name="contact_email" class="form-control" id="contact_email" placeholder="Enter email">
                            <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
                        </div>
                        <div class="form-group">
                            <label for="contact_number">Phone Number</label>
                            <input type="text" name="contact_number" class="form-control" id="contact_number" placeholder="Enter phone number">
                            <small id="numberHelp" class="form-text text-muted">We'll never share your contact with anyone else.</small>
                        </div>
                    <legend>Shipping Address</legend>
                        <div class="form-row">
                            <div class="col">
                                <label for="shipping_firstname">First name</label>
                                <input type="text" name="shipping_firstname" class="form-control" placeholder="John">
                            </div>
                            <div class="col">
                                <label for="shipping_lastname">Last name</label>
                                <input type="text" name="shipping_lastname" class="form-control" placeholder="Doe">
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="shipping_address">Address</label>
                            <input type="text" name="shipping_address" class="form-control" id="shipping_address" placeholder="Address">
                        </div>
                        <div class="form-group">
                            <label for="shipping_address_optional">Address 2</label>
                            <input type="text" name="shipping_address_optional" class="form-control" id="shipping_address_optional" placeholder="Address">
                        </div>
                        <div class="form-group">
                            <label for="shipping_city">City</label>
                            <input type="text" name="shipping_city" class="form-control" id="shipping_city" placeholder="City">
                        </div>
                        <div class="form-row">
                            <div class="col">
                                <label for="shipping_country">Country</label>
                                <input type="text" name="shipping_country" class="form-control" placeholder="Country">
                            </div>
                            <div class="col">
                                <label for="shipping_postal_code">Postal code</label>
                                <input type="text" name="shipping_postal_code" class="form-control" placeholder="Postal code">
                            </div>
                        </div>
                        <br/>
                        <button type="submit" class="btn btn-primary">Submit</button>
                </form>
            </div>
            <div class="col-md-5">
                <legend>Order Details</legend>
                <table class="table">
                    <tbody>
                        {% for items in data.order.line_items %}
                        <tr>
                            <td>
                                {{ items.title }}<br/>
                                {{ items.variant_title }}
                            </td>
                            <td class="text-right">{% if items.price_set.shop_money.currency_code == 'AUD' %}${% endif %}{{ items.price }}</td>
                        </tr>
                        {% endfor %}
                        <tr>
                            <td>Subtotal</td>
                            <td class="text-right">{% if data.order.currency == 'AUD' %}${% endif %}{{data.order.subtotal_price_set.presentment_money.amount}}</td>
                        </tr>
                        <tr>
                            <td>Discount</td>
                            <td class="text-right">{% if data.order.currency == 'AUD' %}${% endif %}{{data.order.total_discounts_set.presentment_money.amount}}</td>
                        </tr>
                        <tr>
                            <td>Shipping</td>
                            <td class="text-right">{% if data.order.currency == 'AUD' %}${% endif %}{{data.order.total_shipping_price_set.presentment_money.amount}}</td>
                        </tr>
                        <tr>
                            <td>Total</td>
                            <td class="text-right">
                                <span>{{data.order.total_price_set.presentment_money.currency_code}}</span>
                                {% if data.order.currency == 'AUD' %}${% endif %}{{data.order.total_price_set.presentment_money.amount}}
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</section>
{% block 'footer' %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                Copyright @ 2019, sixt4
            </div>
        </div>
    </div>
{% endblock %}